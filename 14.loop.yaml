- name: loop demo
  hosts: web
  connection: local
  tasks: 
  - name: the loop to print
    ansible.builtin.debug:
     msg: " Hello {{item}}"
    loop:
    - Ramesh
    - Raju
    - Balu
    - srinu
